version: '3'
services:
  spring-boot-app:
    build:
      context: spring-boot-service
    ports:
      - "8080:8080"

  react-app:
    build:
      context: react-frontend
    ports:
      - "8081:3000"
